객체
- 자바스크립트의 객체는 크게 내장객체, 브라우저 객체 모델(BOM : Browser Object Model),
	문서 객체 모델(DOM : Document Object Model):로 나누어짐
	
1. 내장객체
	- 자바스크립트 엔진에 내장되어 있으며, 필요한 경우 생성해서 사용할 수 있음.
	- 내장 객체는 문자(String), 날짜(Date), 배열(Array), 수학(Math) 객체 등이 있음.
	
2. 브라우저 객체 모델(BOM : Browser Object Model)
	- 브라우저에 계층적으로 내장되어 있는 객체들을 말함.
	- 브라우저 객체 모델에는 window, screen, location, history, navigation 객체 등이 있음

3. 문서 객체 모델(DOM : Document Object Model)
	- HTUML 문서 구조를 말함.
	- 예를 든다면 <html>이 있으며, 그 하위에는 <head>와 <body>가 있음.
		<head> 하위에는 <meta>와 <title>가 있음.
		html의 자식은 head, body /// 자손은 head, body, meta, title 모두
	- 자바스크립트의 문서 객체 모델은 IE 8 이하에서는 사용이 힘들다는 단점이 있음.